# Vocabulary Prompt

Generate `vocabulary.yaml` for an Italian learning topic for Brazilian Portuguese speakers.

Use valid YAML. Include useful topic vocabulary only. Use Italian in `italian` and `example`; use Brazilian Portuguese in translations and notes.

Use this shape: `topic`, `level`, and `words`, where each word has `italian`, `translation`, `example`, `example_translation`, and optional `note`.
