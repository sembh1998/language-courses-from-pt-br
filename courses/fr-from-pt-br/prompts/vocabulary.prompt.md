# Vocabulary Prompt

Generate `vocabulary.yaml` for a French learning topic for Brazilian Portuguese speakers.

Use valid YAML. Include useful topic vocabulary only. Use French in `french` and `example`; use Brazilian Portuguese in translations and notes.

Use this shape: `topic`, `level`, and `words`, where each word has `french`, `translation`, `example`, `example_translation`, and optional `note`.
