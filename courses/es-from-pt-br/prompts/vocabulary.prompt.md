# Vocabulary Prompt

Generate `vocabulary.yaml` for a Spanish learning topic for Brazilian Portuguese speakers.

Use valid YAML. Include useful topic vocabulary only. Use Spanish in `spanish` and `example`; use Brazilian Portuguese in translations and notes.

Use this shape: `topic`, `level`, and `words`, where each word has `spanish`, `translation`, `example`, `example_translation`, and optional `note`.
