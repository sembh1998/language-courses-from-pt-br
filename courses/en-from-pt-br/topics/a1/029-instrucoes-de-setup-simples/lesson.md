# Instruções de setup simples

Nível: A1

## Objetivo

Entender e dar instruções básicas de setup em inglês usando comandos comuns, verbos simples e palavras de sequência como `first`, `then`, `next` e `finally`.

## Explicação

Em equipes de software, instruções de setup costumam ser curtas e diretas. No nível A1, foque em frases no imperativo: verbo no começo, sem sujeito.

- `Install Node.` = Instale o Node.
- `Open the terminal.` = Abra o terminal.
- `Run the command.` = Rode o comando.
- `Clone the repository.` = Clone o repositório.

Para mostrar ordem, use marcadores simples:

- `First` = primeiro
- `Then` = então, depois
- `Next` = em seguida
- `Finally` = finalmente

## Exemplos

| Inglês | Tradução | Observação |
|---|---|---|
| First, install Node. | Primeiro, instale o Node. | `First` marca o primeiro passo. |
| Then, open the terminal. | Depois, abra o terminal. | `Then` continua a sequência. |
| Next, clone the repository. | Em seguida, clone o repositório. | `Repository` é comum em Git. |
| Finally, run the command. | Finalmente, rode o comando. | `Finally` marca o último passo. |

## Erros comuns

- Não traduza `run` como correr em contexto técnico. Em `run a command`, o sentido é rodar/executar.
- Não diga `install the Node` quando estiver falando da ferramenta. O natural é `install Node`.
- Não misture a ordem: `finally` deve aparecer no fim, não no começo.

## Prática guiada

Leia a sequência e observe os verbos no começo:

1. `First, install Node.`
2. `Then, open the terminal.`
3. `Next, clone the repository.`
4. `Finally, run the command.`

Agora troque apenas o comando final:

- `Finally, run npm install.`
- `Finally, run npm test.`

## Resumo

Use verbos diretos para instruções de setup: `install`, `open`, `run`, `clone`. Use `first`, `then`, `next` e `finally` para organizar passos simples.
