# Vocabulary Prompt

Generate `vocabulary.yaml` for an English learning topic for Brazilian Portuguese speakers.

Use valid YAML. Include useful topic vocabulary only. Use English in `english` and `example`; use Brazilian Portuguese in translations and notes.

Use this shape: `topic`, `level`, and `words`, where each word has `english`, `translation`, `example`, `example_translation`, and optional `note`.
